# ADR-0116 D2 — FROZEN tenant-home removal sites (gate CHECK 11).
#
# Format: <file>:<fn>  (line numbers deliberately dropped so a benign line
#   shift does not churn this set; the FILE and FUNCTION are what is frozen.)
#
# CHECK 11 re-derives the TENANT_HOME set with
# tools/adr0116_evidence_removal_scan.awk and FAILS if a site appears that is
# not listed here. The set may only SHRINK. A site leaves the set by routing
# its removals through `aberp_snapshot::guarded_remove` /
# `is_protected_evidence` — at which point the scanner classifies it GUARDED
# and it must be deleted from this file.
#
# ## Why this check exists
#
# Recovery evidence lives as SIBLINGS of the live DB inside a tenant home,
# where `plan_retention` cannot see it. The protection the brief asked for
# ("*CORRUPT*/*RECOVERY*/*DEFORK*/*PRE-* must never be pruned") was therefore
# satisfied BY ACCIDENT: the pruner is structurally blind to those files rather
# than deliberately protective of them, and — as ADR-0116 D2.3 puts it — "a
# refusal inside `prune` alone protects nothing", because `prune` only ever
# touches the snapshot store.
#
# The hazard is a helper that enumerates a tenant home and unlinks by name or
# prefix. That is not hypothetical: `recover::cleanup_siblings_with_infix`
# already had exactly that shape and met no guard at all. It is safe TODAY only
# because the two infixes its two call sites pass happen not to collide with an
# evidence family — a property of the call sites, not of the function. It now
# routes through `guarded_remove`, which is why it is absent below.
#
# ## Every site below is legitimate, and here is the specific reason
#
# Seven of the nine remove artefacts that are on the guard's own LIVE
# allow-list, so routing them through `guarded_remove` would change nothing but
# noise. They are frozen rather than migrated because they are the durability
# primitives themselves, and each is pinned by its own tests. The two added by
# the F8 fix are named separately below.
#
#   crash_safe.rs:atomic_install          — drops the staged file's WAL and the
#                                           target's stale WAL around the
#                                           rename. `<db>.wal` is live.
#   crash_safe.rs:clear_install_intent    — removes `<db>.install-intent`,
#                                           a live journal file.
#   crash_safe.rs:durable_checkpoint      — clears its own `.creating-*`
#                                           staging + export dir (transients).
#   crash_safe.rs:resume_pending_install  — clears a stale WAL while resuming a
#                                           journalled install.
#   recover.rs:cleanup_temp               — removes a `.recover-*` temp + WAL.
#   take.rs:restore_into                  — clears its `.restoring` staging and
#                                           the target's WAL BEFORE the install
#                                           (ADR-0116 D3.1 — the WAL-first
#                                           ordering that closes G2's window).
#   take.rs:take_snapshot_with            — clears a crashed `*.partial` export
#                                           directory in the snapshot store.
#
# ## The two sites the F8 fix made VISIBLE (they were never absent, only
# ## unclassified)
#
# The scanner used to decide "does this fn reach a tenant home?" purely from
# tokens in the fn body. `evidence.rs::archive_then_remove` — the sanctioned
# release path, the ONE function in the tree whose job is unlinking recovery
# evidence from a live tenant home — carries none of them: it works through
# `artefact.path` and `dest`. So it was classified OTHER, sat OUTSIDE this
# may-only-shrink freeze, and a later change that weakened it (or a new removal
# added inside it) would have kept CHECK 11 green. `crash_safe::cleanup_stale`
# was invisible for the same reason.
#
# Three files are now tenant-home-reaching BY FILE (evidence.rs, recover.rs,
# crash_safe.rs) rather than by hoping a fn names the right local, which is
# what surfaced both.
#
#   evidence.rs:archive_then_remove       — ADR-0116 D2's sanctioned release
#                                           path. It unlinks evidence from a
#                                           live tenant home DELIBERATELY, and
#                                           only after copying it, re-reading
#                                           the copy from disk, SHA-comparing
#                                           it and fsync-ing it; credential
#                                           material and directories are
#                                           refused outright. Routing it
#                                           through `guarded_remove` would make
#                                           it refuse its own charter. Frozen
#                                           here so a WEAKENING is visible.
#   crash_safe.rs:cleanup_stale           — removes its own `.creating-*`
#                                           staging file/WAL and export dir on
#                                           an aborted durable checkpoint. Live
#                                           transients, allow-listed by name.
#
crates/aberp-snapshot/src/crash_safe.rs:atomic_install
crates/aberp-snapshot/src/crash_safe.rs:cleanup_stale
crates/aberp-snapshot/src/crash_safe.rs:clear_install_intent
crates/aberp-snapshot/src/crash_safe.rs:durable_checkpoint
crates/aberp-snapshot/src/crash_safe.rs:resume_pending_install
crates/aberp-snapshot/src/evidence.rs:archive_then_remove
crates/aberp-snapshot/src/recover.rs:cleanup_temp
crates/aberp-snapshot/src/take.rs:restore_into
crates/aberp-snapshot/src/take.rs:take_snapshot_with
