# ADR-0098 R7 — FROZEN mirror-fork co-occurrence set (gate CHECK 10L-b).
# Format: <file>|<fname>. Derived by tools/adr0098_r7_mirror_fork_scan.awk over the
#   SAME scope/skip as CHECK 10i (apps/aberp/src + modules + crates, minus the
#   sanctioned aberp-db / aberp-snapshot seams and the 7 C2-migrated files).
# Each entry is a RUNTIME function that opens the live tenant DB INDEPENDENTLY of
#   the shared aberp_db::Handle AND rewrites the audit mirror from that separate
#   instance (a `.sync_mirror(` call) — the exact 415/416 stale-head re-fork
#   signature. The gate FAILS if this set GROWS or a NEW file appears; it is a
#   v0.2.6 Handle-migration worklist that may only SHRINK. It is the standing
#   proof that the two seams closed THIS session (restore_from_nav_outgoing.rs::
#   append_backfill_cycle_entry — the boot re-fork — and incoming_invoices.rs::
#   change_status) have LEFT the set and cannot silently regrow the pair.
# NOTE: presence here is NOT "safe" — it is the deferred fork-capable surface,
#   frozen so it cannot expand. The sanctioned post-commit sync_mirror is the
#   Handle's own WriteGuard drop (crates/aberp-db) — never one of these.
#
# ── D-22 (ADR-0114, 2026-08-26): the set is now EMPTY. ──────────────────────
# Nine of the twelve frozen entries were the NAV money-submission CLI commands
# (drain_submission_queue, retry_submission ×2, submit_annulment,
# poll_annulment_ack, observe_receiver_confirmation, recover_from_nav,
# mark_abandoned, request_technical_annulment). D-22 routed all of them onto
# the shared Handle, so their `Ledger::open` + `sync_mirror` pairs are gone and
# they moved into the CHECK 10h C2 set (zero openers, ENFORCED) — the same
# promotion the seven original C2 files got.
#
# The remaining THREE (email_invoice::record_email_audit_entry,
# restore_from_nav_outgoing::process_digest, serve::set_restored_partner_request)
# had ALREADY migrated off before D-22 and were reported by this gate's own
# "(info) migrated off since freeze" line at `main` bae151d — a stale allowance
# for three sites the scanner no longer finds. Three phantom entries in a
# may-only-shrink manifest are three sites that could have regrown a fork
# unnoticed, so they are removed here rather than carried: the manifest's whole
# job is to be the minimal true set.
#
# EMPTY means the strictest possible reading of 10L-b: ANY site the scanner
# reports is new, and fails the build. A line added below must be a
# `<file>|<fname>` pair with its justification written above it.
