{
  "name": "aberp-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "ABERP operator SPA — Svelte 5 frontend mounted inside the Tauri shell. All wire traffic goes through Tauri commands; this package has no HTTP code of its own. Per ADR-0021 §Part B, the backend is reached via HTTPS+JSON, but only the Rust side terminates that TLS.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tauri-apps/cli": "^2.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "tslib": "^2.6.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
